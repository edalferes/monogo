# Configuração da Aplicação monetics

app:
  name: "monetics"
  version: "1.0.0"
  environment: "production"  # development, staging, production
  port: 8080

database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "postgres"  # Set via DB_PASSWORD env var
  name: "monetics_database"  # Set via DB_NAME env var
  ssl_mode: "disable"

jwt:
  secret: "change_me"  # REQUIRED: Set via JWT_SECRET env var
  expiry_hour: 24

logger:
  level: "debug"      # debug, info, warn, error, fatal
  format: "console"     # json, console

root_user:
  username: "root" # Set via ROOT_USERNAME env var
  password: "ZDcxMDUxZmM4M2Jl"  # Set via ROOT_PASSWORD env var